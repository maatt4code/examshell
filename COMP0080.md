# UCL COMP0080: Graphical Models Exam Preparation Guide

This study guide contains a core syllabus cheat sheet, key concept explanations, and step-by-step, mathematically rigorous solutions to the past exam papers found in your workspace: `COMP0080` (2021-22), `COMP0080A7PF / COMP0080A7UF` (2023-24), and the `GM_sample_ex` papers.

---

## 1. Core Syllabus Cheat Sheet & Key Formulas

### 1.1 Bayesian Networks (Directed Graphs) & d-Separation
A Belief Network (or Bayesian Network) factorises a joint distribution as:
$$
P(x_1, \ldots, x_N) = \prod_{i=1}^N P(x_i \mid \text{pa}(x_i))
$$
It must be a **Directed Acyclic Graph (DAG)** to ensure the factorization is valid and normalises to 1 without needing a global partition function.

*   **d-separation:** A path between nodes $A$ and $B$ is **blocked** by a conditioning set $C$ if it contains:
    1.  A chain $A \rightarrow W \rightarrow B$ or $A \leftarrow W \leftarrow B$ where $W \in C$.
    2.  A fork $A \leftarrow W \rightarrow B$ where $W \in C$.
    3.  A collider $A \rightarrow W \leftarrow B$ where $W \notin C$ and no descendant of $W$ is in $C$.
    If all paths between $A$ and $B$ are blocked by $C$, then $A \perp\!\!\perp B \mid C$.

### 1.2 Markov Networks (Undirected Graphs) & Separation
A Markov Network factorises a joint distribution over maximal cliques $C_k$:
$$
P(X) = \frac{1}{Z} \prod_{k} \Phi(C_k), \quad Z = \sum_{X} \prod_{k} \Phi(C_k)
$$
*   **Separation:** In an undirected graph, $A \perp\!\!\perp B \mid C$ if every path between $A$ and $B$ passes through at least one node in $C$.

### 1.3 Hidden Markov Models (HMMs)
An HMM defines a joint distribution:
$$
P(v_{1:T}, h_{1:T}) = P(h_1)P(v_1 \mid h_1) \prod_{t=2}^T P(h_t \mid h_{t-1})P(v_t \mid h_t)
$$
*   **Filtering:** $P(h_t \mid v_{1:t})$ computed via the **Forward Algorithm**:
    1.  *Predict:* $P(h_t \mid v_{1:t-1}) = \sum_{h_{t-1}} P(h_t \mid h_{t-1}) P(h_{t-1} \mid v_{1:t-1})$
    2.  *Update:* $P(h_t \mid v_{1:t}) \propto P(v_t \mid h_t) P(h_t \mid v_{1:t-1})$
*   **Smoothing:** $P(h_t \mid v_{1:T})$ computed via the **Forward-Backward Algorithm**.
*   **Most Likely Sequence:** $\text{argmax}_{h_{1:T}} P(h_{1:T} \mid v_{1:T})$ computed via the **Viterbi Algorithm**.

### 1.4 The Expectation-Maximization (EM) Algorithm
Used to optimize parameters $\theta$ when some variables $H$ are hidden/latent:
*   **E-step:** Compute the posterior distribution of hidden variables: $q_t(H) = P(H \mid V, \theta^{(t)})$
*   **M-step:** Maximize the expected complete log-likelihood: $\theta^{(t+1)} = \text{argmax}_\theta \langle \log P(V, H \mid \theta) \rangle_{q_t(H)}$
*   **Monotonicity Proof:** The variational lower bound (ELBO) is:
$$
\text{ELBO}(q, \theta) = \sum_{H} q(H) \log \frac{P(V, H \mid \theta)}{q(H)} = \log P(V \mid \theta) - KL(q(H) \mid\mid P(H \mid V, \theta))
$$
    1.  The E-step sets $q(H) = P(H \mid V, \theta^{(t)})$, making $KL = 0$ and matching the ELBO to the log-likelihood $\log P(V \mid \theta^{(t)})$.
    2.  The M-step maximizes the ELBO w.r.t. $\theta$, ensuring $\text{ELBO}(q_{t}, \theta^{(t+1)}) \geq \text{ELBO}(q_{t}, \theta^{(t)})$.
    3.  Because the KL divergence is always non-negative:
$$
\log P(V \mid \theta^{(t+1)}) \geq \text{ELBO}(q_{t}, \theta^{(t+1)}) \geq \text{ELBO}(q_{t}, \theta^{(t)}) = \log P(V \mid \theta^{(t)})
$$
        Thus, the log-likelihood is monotonically non-decreasing.

---

## 2. Past Paper Solutions: Independence & Graphs

### 2.1 COMP0080 (2021-22) Question 1(a): Rank-1 Matrix Independence
**Problem:** We have a joint distribution matrix $P$ for two 3-state variables $X_1$ and $X_2$:
$$
P = \begin{pmatrix} 0.06 & 0.37 & 0.12 \\ 0.01 & 0.07 & 0.02 \\ 0.03 & 0.26 & 0.06 \end{pmatrix}
$$
*Are $X_1$ and $X_2$ independent? If not, what is the minimal number of entries to change to make them independent?*

**Solution:**
1.  **Independence Check (Rank-1 condition):**
    For $X_1$ and $X_2$ to be independent, $P$ must have rank 1 (i.e., every row must be a multiple of every other row).
    Let's check the ratios of columns 1 and 3:
    *   Row 1: $0.12 / 0.06 = 2$
    *   Row 2: $0.02 / 0.01 = 2$
    *   Row 3: $0.06 / 0.03 = 2$
    The ratios of columns 1 and 3 are consistent. Now check columns 1 and 2:
    *   Row 1: $0.37 / 0.06 \approx 6.17$
    *   Row 2: $0.07 / 0.01 = 7.00$
    *   Row 3: $0.26 / 0.03 \approx 8.67$
    Because these column ratios differ, $P$ is not rank-1. Thus, $X_1$ and $X_2$ are **not independent**.

2.  **Minimal Entries to Change:**
    *   **Can we change 1 entry?**
        If we change only 1 entry, then at least two rows must remain completely untouched. If the matrix becomes independent, those two untouched rows must be multiples of each other. However, no two rows in the original matrix $P$ are multiples of each other:
$$
\text{Row 1} \not\propto \text{Row 2}, \quad \text{Row 2} \not\propto \text{Row 3}, \quad \text{Row 1} \not\propto \text{Row 3}
$$
        Therefore, changing 1 entry is insufficient.
    *   **Can we change 2 entries?**
        Yes! If we set the column proportion to be exactly $1 : 7 : 2$ (consistent with Row 2, which is $[0.01, 0.07, 0.02]$ and sums to $0.10$):
        *   **Row 1:** Keep $P_{11}=0.06$ and $P_{13}=0.12$ unchanged. Since $P_{13}/P_{11} = 2$, this matches the $1 : 7 : 2$ proportion. To make the entire row match, set $P_{12} = 7 \times P_{11} = 0.42$. The row is now $[0.06, 0.42, 0.12]$ (sum = $0.60$).
        *   **Row 3:** Keep $P_{31}=0.03$ and $P_{33}=0.06$ unchanged. Set $P_{32} = 7 \times P_{31} = 0.21$. The row is now $[0.03, 0.21, 0.06]$ (sum = $0.30$).
        *   **Row 2:** Keep all entries $[0.01, 0.07, 0.02]$ unchanged (sum = $0.10$).
    *   **Verification:**
        The new matrix $Q$ is:
$$
Q = \begin{pmatrix} 0.06 & 0.42 & 0.12 \\ 0.01 & 0.07 & 0.02 \\ 0.03 & 0.21 & 0.06 \end{pmatrix}
$$
        *   **Rank-1:** Every row is a scalar multiple of $v^T = [0.1, 0.7, 0.2]$.
        *   **Normalization:** Sum of all entries $= 0.60 + 0.10 + 0.30 = 1.00$.
        *   **Changes:** Exactly **2** entries were changed ($P_{12}: 0.37 \rightarrow 0.42$ and $P_{32}: 0.26 \rightarrow 0.21$).
    **Answer:** The minimal number of entries to change is **2**.

### 2.2 COMP0080 (2021-22) Question 1(d) & COMP0080A7PF Question 1(c): Marginal Graph Independence
**Problem:** *Consider a Markov Network cycle of 6 variables: $X_1 - X_2 - X_3 - X_6 - X_5 - X_4 - X_1$. Write down all marginal and conditional independence statements that are true for the marginal distribution $P(X_2, X_4, X_6)$.*

**Solution:**
In a Markov Network, conditional independence $A \perp\!\!\perp B \mid C$ holds if and only if all paths between $A$ and $B$ are blocked by $C$.
1.  **Marginal Independences (conditioning set $C = \emptyset$):**
    *   $X_2$ and $X_4$ are connected by the path $X_2 - X_1 - X_4$. (Not independent)
    *   $X_2$ and $X_6$ are connected by the path $X_2 - X_3 - X_6$. (Not independent)
    *   $X_4$ and $X_6$ are connected by the path $X_4 - X_5 - X_6$. (Not independent)
2.  **Conditional Independences (conditioning on one variable):**
    *   $X_2 \perp\!\!\perp X_4 \mid X_6$: The path $X_2 - X_1 - X_4$ does not pass through $X_6$, so it remains unblocked. (Not conditionally independent)
    *   $X_2 \perp\!\!\perp X_6 \mid X_4$: The path $X_2 - X_3 - X_6$ does not pass through $X_4$, so it remains unblocked. (Not conditionally independent)
    *   $X_4 \perp\!\!\perp X_6 \mid X_2$: The path $X_4 - X_5 - X_6$ does not pass through $X_2$, so it remains unblocked. (Not conditionally independent)

**Answer:** There are **no** marginal or conditional independence statements that hold true for the marginal distribution $P(X_2, X_4, X_6)$.

### 2.3 COMP0080A7PF (2023-24) Question 1(d): Factor Graph Independences
**Problem:** *Compare the independence statements encoded by four models: (1) DAG $A \rightarrow C \leftarrow B$, (2) Undirected $A - C - B$, (3) Factor Graph with three pairwise factors $f(A,B)$, $f(B,C)$, $f(C,A)$, (4) Factor Graph with a single factor $f(A,B,C)$.*

**Solution:**
*   **Model 1 (DAG collider):** Encodes $A \perp\!\!\perp B$ marginally.
*   **Model 2 (Undirected line):** Encodes $A \perp\!\!\perp B \mid C$ conditionally.
*   **Model 3 (Pairwise triangle):** The factorization is $P(A, B, C) \propto f(A,B) f(B,C) f(C,A)$. Since every pair is directly connected by a factor, no variables can be separated. It encodes **no** independence statements.
*   **Model 4 (Single factor):** The factorization is $P(A, B, C) \propto f(A, B, C)$. This is the most general joint distribution and encodes **no** independence statements.

**Answer:** Models 3 and 4 encode the exact same set of independence statements (the empty set).

---

## 3. Past Paper Solutions: Inference & Learning

### 3.1 COMP0080 (2021-22) Question 2(b): HMM Filtering
**Problem:** An HMM has parameters:
*   Initial state: $P(h_1 = 1) = 0.5$ (thus $P(h_1 = 0) = 0.5$)
*   Transition matrix: $T = \begin{pmatrix} 0.5 & 0.8 \\ 0.5 & 0.2 \end{pmatrix}$, where $T_{ij} = P(h_t = i \mid h_{t-1} = j)$
*   Emission matrix: $B = \begin{pmatrix} 0.3 & 0.5 \\ 0.6 & 0.0 \\ 0.1 & 0.5 \end{pmatrix}$, where $B_{ki} = P(v_t = k \mid h_t = i)$
*   Observed sequence: $v_{1:10} = [0, 1, 0, 2, 0, 2, 1, 0, 2, 0]$

#### Part (iii): *Is $h_1$ independent of $h_{10}$ in $P(h_{1:10} \mid v_{1:10})$?*
**Solution:**
Look at the emission matrix: $P(v_t = 1 \mid h_t = 1) = 0$.
If we observe $v_t = 1$, the hidden state $h_t$ must be $0$ with probability $1$.
In our sequence, $v_2 = 1$ and $v_7 = 1$. This means:
$$
P(h_2 = 0 \mid v_{1:10}) = 1.0, \quad P(h_7 = 0 \mid v_{1:10}) = 1.0
$$
Because the intermediate states $h_2$ and $h_7$ are known with absolute certainty (deterministic), they block the flow of information between $h_1$ and $h_{10}$. Any path in the posterior Markov chain from $h_1$ to $h_{10}$ must pass through these fixed nodes.
**Answer:** Yes, $h_1$ is conditionally independent of $h_{10}$ given the observed sequence, because the observations $v_2=1$ and $v_7=1$ force $h_2=0$ and $h_7=0$ deterministically, d-separating the chain.

#### Part (iv): *Compute $P(h_8 \mid v_{1:8})$ and $P(h_9 \mid v_{1:9})$ without running the full forward algorithm.*
**Solution:**
Since $v_7 = 1$, we know $P(h_7 = 0 \mid v_{1:7}) = 1$ and $P(h_7 = 1 \mid v_{1:7}) = 0$. We can start our forward updates directly from $t=7$:

1.  **Compute $P(h_8 \mid v_{1:8})$:**
    *   *Predict state at $t=8$ given $v_{1:7}$:*
$$
P(h_8 = i \mid v_{1:7}) = \sum_{j} P(h_8 = i \mid h_7 = j) P(h_7 = j \mid v_{1:7}) = T_{i0}
$$
        Thus:
$$
P(h_8 = 0 \mid v_{1:7}) = T_{00} = 0.5, \quad P(h_8 = 1 \mid v_{1:7}) = T_{10} = 0.5
$$
    *   *Update with observation $v_8 = 0$:*
$$
P(h_8 = i \mid v_{1:8}) \propto P(v_8 = 0 \mid h_8 = i) P(h_8 = i \mid v_{1:7})
$$
        *   For $h_8 = 0$: $B_{00} \times P(h_8=0 \mid v_{1:7}) = 0.3 \times 0.5 = 0.15$
        *   For $h_8 = 1$: $B_{01} \times P(h_8=1 \mid v_{1:7}) = 0.5 \times 0.5 = 0.25$
        *   Normalize ($0.15 + 0.25 = 0.40$):
$$
P(h_8 = 0 \mid v_{1:8}) = \frac{0.15}{0.40} = 0.375, \quad P(h_8 = 1 \mid v_{1:8}) = \frac{0.25}{0.40} = 0.625
$$
    **Answer:** $P(h_8 \mid v_{1:8}) = [0.375, 0.625]^T$.

2.  **Compute $P(h_9 \mid v_{1:9})$:**
    *   *Predict state at $t=9$ given $v_{1:8}$:*
$$
P(h_9 = 0 \mid v_{1:8}) = T_{00} P(h_8=0 \mid v_{1:8}) + T_{01} P(h_8=1 \mid v_{1:8}) = 0.5(0.375) + 0.8(0.625) = 0.1875 + 0.5 = 0.6875
$$
$$
P(h_9 = 1 \mid v_{1:8}) = T_{10} P(h_8=0 \mid v_{1:8}) + T_{11} P(h_8=1 \mid v_{1:8}) = 0.5(0.375) + 0.2(0.625) = 0.1875 + 0.125 = 0.3125
$$
    *   *Update with observation $v_9 = 2$:*
$$
P(h_9 = i \mid v_{1:9}) \propto P(v_9 = 2 \mid h_9 = i) P(h_9 = i \mid v_{1:8})
$$
        *   For $h_9 = 0$: $B_{20} \times P(h_9=0 \mid v_{1:8}) = 0.1 \times 0.6875 = 0.06875$
        *   For $h_9 = 1$: $B_{21} \times P(h_9=1 \mid v_{1:8}) = 0.5 \times 0.3125 = 0.15625$
        *   Normalize ($0.06875 + 0.15625 = 0.225$):
$$
P(h_9 = 0 \mid v_{1:9}) = \frac{0.06875}{0.225} = \frac{11}{36} \approx 0.3056
$$
$$
P(h_9 = 1 \mid v_{1:9}) = \frac{0.15625}{0.225} = \frac{25}{36} \approx 0.6944
$$
    **Answer:** $P(h_9 \mid v_{1:9}) = [0.3056, 0.6944]^T$.

### 3.2 COMP0080A7PF (2023-24) Question 2(c): Randomized Response Survey EM
**Problem:** *Respondents answer a sensitive question. First toss a biased coin $p(H_1=1)=p_1$. If heads, answer truthfully ($Y=T$, with $p(T=1)=p_2$). If tails, toss a fair coin $p(H_2=1)=0.5$ and respond Yes if heads, No if tails.*

#### Part (ii): *If $p_1 = 0.5$, what is the posterior probability that the person answered truthfully (i.e. $H_1=1$) given they answered Yes ($Y=1$)?*
**Solution:**
By Bayes' rule:
$$
P(H_1 = 1 \mid Y = 1) = \frac{P(Y=1 \mid H_1=1) P(H_1=1)}{P(Y=1)}
$$
*   $P(H_1 = 1) = 0.5$
*   $P(Y = 1 \mid H_1 = 1) = P(T = 1) = p_2$
*   $P(Y = 1 \mid H_1 = 0) = P(H_2 = 1) = 0.5$
*   $P(Y = 1) = P(Y = 1 \mid H_1 = 1) P(H_1 = 1) + P(Y = 1 \mid H_1 = 0) P(H_1 = 0) = 0.5 p_2 + 0.25 = 0.5(p_2 + 0.5)$
$$
P(H_1 = 1 \mid Y = 1) = \frac{0.5 p_2}{0.5(p_2 + 0.5)} = \frac{p_2}{p_2 + 0.5} = \frac{2p_2}{2p_2 + 1}
$$
#### Part (iv): *Write the EM updates for estimating unknown $p_1$ and $p_2$ from survey data $D$.*
**Solution:**
Let the hidden variables for respondent $i$ be $H_{1,i} \in \{0, 1\}$ (first coin) and $T_i \in \{0, 1\}$ (true state).
1.  **E-step:** Compute the posterior over hidden variables $q_i(h_1, t) = P(H_{1,i} = h_1, T_i = t \mid Y_i, p_1, p_2)$ using current parameters:
    *   **For $Y_i = 1$:**
$$
q_i(1, 1) = \frac{p_1 p_2}{p_1 p_2 + 0.5(1 - p_1)}, \quad q_i(1, 0) = 0
$$
$$
q_i(0, 1) = \frac{0.5(1 - p_1)p_2}{p_1 p_2 + 0.5(1 - p_1)}, \quad q_i(0, 0) = \frac{0.5(1 - p_1)(1 - p_2)}{p_1 p_2 + 0.5(1 - p_1)}
$$
    *   **For $Y_i = 0$:**
$$
q_i(1, 1) = 0, \quad q_i(1, 0) = \frac{p_1(1 - p_2)}{p_1(1 - p_2) + 0.5(1 - p_1)}
$$
$$
q_i(0, 1) = \frac{0.5(1 - p_1)p_2}{p_1(1 - p_2) + 0.5(1 - p_1)}, \quad q_i(0, 0) = \frac{0.5(1 - p_1)(1 - p_2)}{p_1(1 - p_2) + 0.5(1 - p_1)}
$$
2.  **M-step:** Maximize the expected complete log-likelihood to update $p_1$ and $p_2$:
$$
p_1^{(t+1)} = \frac{1}{n} \sum_{i=1}^n \left( q_i(1, 1) + q_i(1, 0) \right)
$$
$$
p_2^{(t+1)} = \frac{1}{n} \sum_{i=1}^n \left( q_i(1, 1) + q_i(0, 1) \right)
$$
#### Part (v): *With $D = (\text{yes}, \text{yes}, \text{yes}, \text{no})$, is the MLE unique if both $p_1$ and $p_2$ are unknown?*
**Solution:**
The likelihood of a single observation $Y_i$ is determined entirely by $\theta = P(Y_i = 1) = p_1 p_2 + 0.5(1 - p_1)$.
With 3 "Yes" out of 4 observations, the MLE for the single aggregate probability $\theta$ is uniquely:
$$
\hat{\theta} = \frac{3}{4} = 0.75
$$
Therefore, any parameter pair $(p_1, p_2)$ that satisfies:
$$
p_1 p_2 + 0.5(1 - p_1) = 0.75 \implies p_1(p_2 - 0.5) = 0.25 \implies p_2 = 0.5 + \frac{0.25}{p_1}
$$
will yield the maximum likelihood. Since both $p_1, p_2 \in [0, 1]$, any $p_1 \in [0.5, 1]$ paired with $p_2 = 0.5 + \frac{0.25}{p_1}$ is a valid MLE (e.g. $p_1=0.5, p_2=1.0$ or $p_1=1.0, p_2=0.75$).
**Answer:** No, the MLE is not unique. There are infinite parameter pairs $(p_1, p_2)$ that maximize the likelihood because the model is underidentified.

---

## 4. Past Paper Solutions: Approximate Inference & Sampling

### 4.1 COMP0080A7PF (2023-24) Question 3(a): Variational Free Energy
**Problem:** *For the triangular Markov Network with potentials $\Phi(X_i, X_j) = 2$ if $X_i=X_j$, else $1$, derive the Free Energy expression under a fully factorised variational distribution $q(X) = \prod_{i=1}^3 q_i(X_i)$, where $q_i(X_i=1) = \mu_i$.*

**Solution:**
The variational Free Energy $F(q)$ is defined as:
$$
F(q) = -H(q) - \langle \log P^*(X) \rangle_q
$$
1.  **Entropy $H(q)$:**
    Because $q$ is fully factorised, its entropy is the sum of the coordinate entropies:
$$
H(q) = \sum_{i=1}^3 \left[ -\mu_i \log \mu_i - (1 - \mu_i) \log(1 - \mu_i) \right]
$$
2.  **Expected Log Potentials $\langle \log P^*(X) \rangle_q$:**
    The unnormalized potential is $P^*(X) = \prod_{i < j} \Phi(X_i, X_j)$. Taking the log:
$$
\log P^*(X) = \sum_{i < j} \log \Phi(X_i, X_j) = (\log 2) \sum_{i < j} \mathbb{I}(X_i = X_j)
$$
    Taking the expectation w.r.t. $q$:
$$
\langle \log P^*(X) \rangle_q = (\log 2) \sum_{i < j} Q(X_i = X_j)
$$
    Since $X_i$ and $X_j$ are independent under $q$:
$$
Q(X_i = X_j) = q(X_i=1)q(X_j=1) + q(X_i=0)q(X_j=0) = \mu_i \mu_j + (1 - \mu_i)(1 - \mu_j)
$$
$$
\langle \log P^*(X) \rangle_q = (\log 2) \sum_{i < j} \left[ \mu_i \mu_j + (1 - \mu_i)(1 - \mu_j) \right]
$$
**Answer:** The variational Free Energy is:
$$
F(q) = \sum_{i=1}^3 \left[ \mu_i \log \mu_i + (1 - \mu_i) \log(1 - \mu_i) \right] - (\log 2) \sum_{i < j} \left[ \mu_i \mu_j + (1 - \mu_i)(1 - \mu_j) \right]
$$
### 4.2 COMP0080 (2021-22) Question 3(b) & COMP0080A7PF Question 3(b): Rejection Sampling on Lattice
**Problem:** *Consider a Markov Network on an $M \times N$ lattice where binary variables $X_i \in \{0, 1\}$ have edge potentials $P_{ij}(X_i, X_j) = e^{-\mathbb{I}(X_i \neq X_j)}$. We use a uniform proposal distribution $Q(X) = 2^{-V}$ where $V = M \times N$. Compute the average acceptance rates for: (i) $M=2, N=1$, (ii) $M=2, N=2$.*

**Solution:**
Rejection sampling requires a constant $c$ such that $P^*(X) \leq c Q(X)$ for all $X$. To maximize acceptance rate, we choose:
$$
c = \max_{X} \frac{P^*(X)}{Q(X)} = 2^V \max_{X} P^*(X)
$$
Since $\max_{X} P^*(X) = 1$ (when all lattice nodes have the same value, resulting in 0 mismatched edges), we have $c = 2^V$.
The acceptance probability for a proposal $X$ is $a(X) = \frac{P^*(X)}{c Q(X)} = P^*(X)$.
The average acceptance rate is:
$$
\text{Rate} = \sum_{X} a(X) Q(X) = 2^{-V} \sum_{X} P^*(X) = 2^{-V} Z
$$
1.  **Case (i) $M=2, N=1$ ($V=2$, $1$ edge):**
    There are 4 configurations for $(X_1, X_2)$:
    *   $X_1 = X_2$ ($00, 11$): $P^*(X) = 1$. (2 configs)
    *   $X_1 \neq X_2$ ($01, 10$): $P^*(X) = e^{-1}$. (2 configs)
$$
Z = 2(1) + 2(e^{-1}) = 2 + 2e^{-1} \approx 2.7358
$$
$$
\text{Rate} = 2^{-2} Z = \frac{2 + 2e^{-1}}{4} = \frac{1 + e^{-1}}{2} \approx 0.6839
$$
2.  **Case (ii) $M=2, N=2$ ($V=4$, $4$ edges in a square):**
    There are 16 configurations. Mismatched edges must occur in even quantities for a closed cycle:
    *   **0 mismatches:** $0000, 1111$ $\implies P^*(X) = 1$. (2 configs)
    *   **2 mismatches:** (e.g. three nodes equal and one different, or adjacent pairs of unequal nodes) $\implies P^*(X) = e^{-2}$. (12 configs)
    *   **4 mismatches:** (diagonal alternating configurations $0101$ and $1010$) $\implies P^*(X) = e^{-4}$. (2 configs)
$$
Z = 2(1) + 12(e^{-2}) + 2(e^{-4}) \approx 2 + 1.6240 + 0.0366 = 3.6607
$$
$$
\text{Rate} = 2^{-4} Z = \frac{3.6607}{16} \approx 0.2288
$$
**Answer:** Rejection sampling is generally a **bad idea** for larger lattices because the acceptance rate $2^{-V}Z$ decays exponentially with the number of variables $V$.

### 4.3 COMP0080 (2021-22) Question 3(c) & COMP0080A7PF Question 3(c): MCMC Detailed Balance
**Problem:** *Explain detailed balance. Is it a necessary condition for a sampler? Provide a counter-example where detailed balance does not hold but the MCMC sampler is still valid.*

**Solution:**
*   **Detailed Balance:** $P(x) T(x \rightarrow x') = P(x') T(x' \rightarrow x)$ for all $x, x'$, where $T$ is the transition probability.
*   **Is it necessary?** No, detailed balance is a **sufficient** but not necessary condition for $P(x)$ to be the stationary distribution. The necessary and sufficient condition is the **global balance equation**:
$$
\sum_{x} P(x) T(x \rightarrow x') = P(x') \quad \forall x'
$$
*   **Counter-example (Non-reversible Markov chain):**
    Consider a 3-state system $\{1, 2, 3\}$ with a uniform target distribution $P(1)=P(2)=P(3)=1/3$.
    Define a cyclic deterministic transition matrix:
$$
T(1 \rightarrow 2) = 1, \quad T(2 \rightarrow 3) = 1, \quad T(3 \rightarrow 1) = 1
$$
    *   **Global Balance Check:**
$$
\sum_{x} P(x) T(x \rightarrow 1) = P(3) T(3 \rightarrow 1) = \frac{1}{3} \cdot 1 = \frac{1}{3} = P(1)
$$
        (Holds for all states; thus $P(x) = 1/3$ is indeed the stationary distribution).
    *   **Detailed Balance Check:**
$$
P(1) T(1 \rightarrow 2) = \frac{1}{3} \cdot 1 = \frac{1}{3}
$$
$$
P(2) T(2 \rightarrow 1) = \frac{1}{3} \cdot 0 = 0
$$
        Since $\frac{1}{3} \neq 0$, detailed balance does not hold.
    *(Another practical example is the **systematic scan Gibbs sampler**, where coordinates are updated in a fixed sequential order. Because the order of operations is cyclic/asymmetric, detailed balance does not hold for the overall transition step, but the chain still converges to the correct target distribution.)*

---

## 5. Complete Derivations for the Sample Exam (GM_sample_ex.pdf)

### 5.1 Graph Theory Bookwork & Proofs
#### Part (a.iii): *Explain why any distribution can be written as a belief network.*
By the chain rule of probability, any joint distribution can be decomposed without loss of generality as:
$$
P(x_1, \ldots, x_N) = P(x_1) P(x_2 \mid x_1) P(x_3 \mid x_1, x_2) \ldots P(x_N \mid x_1, \ldots, x_{N-1})
$$
This decomposition is identical to the factorization of a belief network over a fully connected Directed Acyclic Graph (DAG) under a chosen node ordering $\{x_1, \dots, x_N\}$. For each node $x_i$, its parents are $\text{pa}(x_i) = \{x_1, \dots, x_{i-1}\}$. Because the ordering prevents cycles (all edges point from lower indices to higher indices), this fully connected DAG is always valid, proving any distribution can be written as a belief network.

#### Part (a.iv): *Lack of edges vs presence of edges in a DAG.*
*   **Lack of an edge:** If there is no edge between two variables $A$ and $B$, they are conditionally independent given some set of other nodes (specifically, $A \perp\!\!\perp B \mid \text{pa}(A)$ if $B$ is not a descendant of $A$). Since d-separation implies independence, the absence of an edge guarantees that at least one conditional/marginal independence statement holds across all distributions consistent with the DAG.
*   **Presence of an edge:** An edge $A \rightarrow B$ means $A$ is in the parent set of $B$, so $P(B \mid \text{pa}(B))$ is part of the factorization. However, the graph only represents the *set of distributions* that factorise this way. It does not force $B$ to depend on $A$. For example, a distribution where $P(B \mid A) = P(B)$ is perfectly consistent with the factorization, meaning $A$ and $B$ are independent despite the edge. Hence, the presence of an edge does not imply dependence.

#### Part (b.ii): *Show that any distribution can be written as a Markov network.*
Any joint distribution $P(x_1, \dots, x_N)$ can be represented by a fully connected undirected graph (a single clique containing all variables). In this case, we define a single maximal clique potential $\Phi(x_1, \dots, x_N) = P(x_1, \dots, x_N)$, and the partition function is $Z = 1$. This satisfies the Markov Network factorization definition, proving any distribution can be represented as a Markov Network.

#### Part (b.iii): *Moral skeleton counter-example.*
If DAG $A$ contains a collider/V-structure (e.g. $A \rightarrow B \leftarrow C$, where $A$ and $C$ are independent parents of $B$), the DAG encodes $A \perp\!\!\perp C$ and $A \not\perp\!\!\perp C \mid B$. 
The undirected skeleton $B$ contains edges $A - B$ and $B - C$ (no edge between $A$ and $C$). In this Markov Network, the only path between $A$ and $C$ goes through $B$, which encodes $A \perp\!\!\perp C \mid B$ and $A \not\perp\!\!\perp C$ marginally.
These independence statements are opposite. To represent the DAG as a Markov Network, we must "marry the parents" (moralization) by adding an edge $A - C$, which changes the adjacency matrix. Thus, not every belief net can be represented by a Markov Network with the skeleton adjacency matrix $B$.

---

### 5.2 HMM Standard Derivations
Consider the Hidden Markov Model:
$$
P(v_{1:T}, h_{1:T}) = P(h_1)P(v_1 \mid h_1) \prod_{t=2}^T P(h_t \mid h_{t-1})P(v_t \mid h_t)
$$
#### 1. Filtering Recursion $P(h_t \mid v_{1:t})$:
Using Bayes' rule and the HMM Markov assumptions:
$$
P(h_t \mid v_{1:t}) = \frac{P(v_t \mid h_t, v_{1:t-1}) P(h_t \mid v_{1:t-1})}{P(v_t \mid v_{1:t-1})} = \frac{P(v_t \mid h_t) P(h_t \mid v_{1:t-1})}{P(v_t \mid v_{1:t-1})}
$$
Expanding the prediction term:
$$
P(h_t \mid v_{1:t-1}) = \sum_{h_{t-1}} P(h_t \mid h_{t-1}) P(h_{t-1} \mid v_{1:t-1})
$$
Substituting back, we get the forward recursion:
$$
P(h_t \mid v_{1:t}) = \frac{P(v_t \mid h_t) \sum_{h_{t-1}} P(h_t \mid h_{t-1}) P(h_{t-1} \mid v_{1:t-1})}{\sum_{h_t} P(v_t \mid h_t) \sum_{h_{t-1}} P(h_t \mid h_{t-1}) P(h_{t-1} \mid v_{1:t-1})}
$$
#### 2. Smoothing Recursion $P(h_t \mid v_{1:T})$:
By the HMM conditional independence properties, the future observations $v_{t+1:T}$ are conditionally independent of past observations $v_{1:t}$ given the current state $h_t$:
$$
P(h_t \mid v_{1:T}) \propto P(h_t, v_{1:T}) = P(v_{t+1:T} \mid h_t) P(h_t, v_{1:t})
$$
Let $\alpha_t(h_t) = P(h_t, v_{1:t})$ (forward variable) and $\beta_t(h_t) = P(v_{t+1:T} \mid h_t)$ (backward variable).
*   **Forward update ($\alpha$):** $\alpha_t(h_t) = P(v_t \mid h_t) \sum_{h_{t-1}} P(h_t \mid h_{t-1}) \alpha_{t-1}(h_{t-1})$
*   **Backward update ($\beta$):** 
$$
\beta_t(h_t) = \sum_{h_{t+1}} P(v_{t+1:T}, h_{t+1} \mid h_t) = \sum_{h_{t+1}} P(v_{t+2:T} \mid h_{t+1}) P(v_{t+1} \mid h_{t+1}) P(h_{t+1} \mid h_t) = \sum_{h_{t+1}} \beta_{t+1}(h_{t+1}) P(v_{t+1} \mid h_{t+1}) P(h_{t+1} \mid h_t)
$$
The smoothed marginal is $P(h_t \mid v_{1:T}) = \frac{\alpha_t(h_t) \beta_t(h_t)}{\sum_{h_t} \alpha_t(h_t) \beta_t(h_t)}$.

#### 3. Marginal Likelihood $P(v_{1:T})$:
$$
P(v_{1:T}) = \sum_{h_T} P(h_T, v_{1:T}) = \sum_{h_T} \alpha_T(h_T)
$$
If utilizing the scaling factor version of the forward algorithm where $c_t = P(v_t \mid v_{1:t-1})$, then:
$$
P(v_{1:T}) = \prod_{t=1}^T c_t
$$
#### 4. Prediction $P(v_{T+1} \mid v_{1:T})$:
$$
P(v_{T+1} \mid v_{1:T}) = \sum_{h_{T+1}} P(v_{T+1} \mid h_{T+1}) P(h_{T+1} \mid v_{1:T}) = \sum_{h_{T+1}} P(v_{T+1} \mid h_{T+1}) \left[ \sum_{h_T} P(h_{T+1} \mid h_T) P(h_T \mid v_{1:T}) \right]
$$
#### 5. Pairwise Marginal $P(h_t, h_{t+1} \mid v_{1:T})$:
$$
P(h_t, h_{t+1} \mid v_{1:T}) \propto P(h_t, h_{t+1}, v_{1:T}) = P(h_t, v_{1:t}) P(h_{t+1} \mid h_t) P(v_{t+1} \mid h_{t+1}) P(v_{t+2:T} \mid h_{t+1}) = \alpha_t(h_t) P(h_{t+1} \mid h_t) P(v_{t+1} \mid h_{t+1}) \beta_{t+1}(h_{t+1})
$$
Normalizing by the marginal likelihood:
$$
P(h_t, h_{t+1} \mid v_{1:T}) = \frac{\alpha_t(h_t) P(h_{t+1} \mid h_t) P(v_{t+1} \mid h_{t+1}) \beta_{t+1}(h_{t+1})}{P(v_{1:T})}
$$
#### 6. Viterbi Algorithm (Most likely hidden sequence):
Let $V_t(h_t) = \max_{h_{1:t-1}} P(h_{1:t}, v_{1:t})$ be the probability of the most likely path ending in state $h_t$ at time $t$.
*   **Initialization:** $V_1(h_1) = P(h_1) P(v_1 \mid h_1)$
*   **Recursion:** $V_t(h_t) = P(v_t \mid h_t) \max_{h_{t-1}} \left[ P(h_t \mid h_{t-1}) V_{t-1}(h_{t-1}) \right]$
    Store backpointers: $\psi_t(h_t) = \text{argmax}_{h_{t-1}} \left[ P(h_t \mid h_{t-1}) V_{t-1}(h_{t-1}) \right]$
*   **Termination:** Find the best ending state $h_T^* = \text{argmax}_{h_T} V_T(h_T)$.
*   **Backtracking:** For $t = T-1$ down to 1, find $h_t^* = \psi_{t+1}(h_{t+1}^*)$.

#### 7. Forward-Filtering Backward-Sampling (FFBS):
Used to draw exact independent joint samples from the posterior $P(h_{1:T} \mid v_{1:T})$:
1.  **Forward Pass:** Run the forward algorithm to compute and store the filtered marginals $P(h_t \mid v_{1:t})$ for all $t \in \{1,\dots,T\}$.
2.  **Sampling:**
    *   Sample the final state: $h_T^{(s)} \sim P(h_T \mid v_{1:T})$.
    *   For $t = T-1$ down to 1, sample states sequentially:
$$
h_t^{(s)} \sim P(h_t \mid h_{t+1}^{(s)}, v_{1:T}) = P(h_t \mid h_{t+1}^{(s)}, v_{1:t}) \propto P(h_{t+1}^{(s)} \mid h_t) P(h_t \mid v_{1:t})
$$
---

### 5.3 Sampling & KL Divergence Proofs
#### Gibbs Sampling Detailed Balance:
In Gibbs sampling, coordinate $i$ is updated while holding other coordinates $x_{-i}$ fixed. The transition probability is:
$$
T(x \rightarrow x') = P(x'_i \mid x_{-i}) \mathbb{I}(x'_{-i} = x_{-i})
$$
Multiplying by the target distribution $P(x)$:
$$
P(x) T(x \rightarrow x') = P(x_i, x_{-i}) P(x'_i \mid x_{-i}) \mathbb{I}(x'_{-i} = x_{-i}) = P(x_{-i}) P(x_i \mid x_{-i}) P(x'_i \mid x_{-i}) \mathbb{I}(x'_{-i} = x_{-i})
$$
Now check the reverse direction:
$$
P(x') T(x' \rightarrow x) = P(x'_i, x'_{-i}) P(x_i \mid x'_{-i}) \mathbb{I}(x_{-i} = x'_{-i})
$$
Using the constraint $\mathbb{I}(x_{-i} = x'_{-i})$, we can replace $x'_{-i}$ with $x_{-i}$ in the terms:
$$
P(x') T(x' \rightarrow x) = P(x_{-i}) P(x'_i \mid x_{-i}) P(x_i \mid x_{-i}) \mathbb{I}(x_{-i} = x'_{-i})
$$
Since both products are identical, detailed balance holds.

#### Metropolis-Hastings Detailed Balance:
The transition probability for $x \neq x'$ is $T(x \rightarrow x') = q(x' \mid x) \alpha(x, x')$, where $\alpha(x, x') = \min\left(1, \frac{P(x') q(x \mid x')}{P(x) q(x' \mid x)}\right)$.
$$
P(x) T(x \rightarrow x') = P(x) q(x' \mid x) \min\left(1, \frac{P(x') q(x \mid x')}{P(x) q(x' \mid x)}\right) = \min\left( P(x) q(x' \mid x), P(x') q(x \mid x') \right)
$$
By symmetry, the reverse term yields the same expression:
$$
P(x') T(x' \rightarrow x) = P(x') q(x \mid x') \min\left(1, \frac{P(x) q(x' \mid x)}{P(x') q(x \mid x')}\right) = \min\left( P(x') q(x \mid x'), P(x) q(x' \mid x) \right)
$$
Thus, $P(x) T(x \rightarrow x') = P(x') T(x' \rightarrow x)$, proving detailed balance holds.

#### KL Divergence: Mode-Seeking vs Mean-Seeking:
*   **$KL(P \mid\mid Q) = \sum_x P(x) \log \frac{P(x)}{Q(x)}$ (Mean-seeking / Zero-avoiding):**
    If $P(x) > 0$, we must have $Q(x) > 0$ to avoid an infinite penalty ($\log \frac{P(x)}{0} \rightarrow \infty$). Therefore, the optimal unimodal $Q(x)$ must stretch to cover all modes of a bimodal $P(x)$, resulting in a broad, high-variance approximation that averages over the modes.
*   **$KL(Q \mid\mid P) = \sum_x Q(x) \log \frac{Q(x)}{P(x)}$ (Mode-seeking / Zero-forcing):**
    If $P(x) = 0$ or is very small, we must have $Q(x) \rightarrow 0$ to avoid a large penalty. Therefore, $Q(x)$ is forced to be zero in regions of low probability of $P(x)$. To fit a unimodal $Q(x)$ to a bimodal $P(x)$, $Q(x)$ will collapse and focus on a single mode of $P(x)$, completely ignoring the other mode to maintain a tight fit.

